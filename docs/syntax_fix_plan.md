## План по исправлению синтаксиса и документации

### 1. Базовая сверка с эталоном
- Сравнить текущие директории `supra/move_workspace/lottery_core`, `lottery_support`, `lottery_rewards` с соответствующими файлами в `supra/move_workspace/lottery_backup`.
- Проанализировать синтаксис эталонных файлов, чтобы заранее выявить потенциальные проблемы (например, недопустимые конструкции или расхождения с требованиями текущего Move-компилятора).
- Зафиксировать различия в объявлении структур, модулей, комментариях и сигнатурах функций.

### 2. Коррекция Move-контрактов
- Удалить запрещённые модификаторы (`public` у структур), восстановить допустимые `has`-трейты и порядок объявлений по образцу из бэкапа.
- Проверить сигнатуры функций: убрать лишние `&mut`, вернуть корректный порядок аргументов, синхронизировать возвращаемые типы.
- Убедиться в отсутствии не-ASCII символов и запрещённого синтаксиса (`let mut` и т. п.).

### 3. Проверка дополнительных пакетов
- Повторить шаги для `lottery_support` и `lottery_rewards`, обращая внимание на межмодульные зависимости и названия capability.
- Обновить тестовые утилиты и хелперы, если они расходятся с эталоном.

### 4. Обновление документации
- Перепроверить `docs/testnet_runbook.md`, `SupraLottery/README.md` и связанные инструкции.
- Обновить описания capability, процедур деплоя и сборки, добавить примечания о требованиях Move-компилятора (ASCII, отсутствие модификаторов, формат комментариев).
- Синхронизировать примеры команд с актуальными путями и пакетами.


