## План по исправлению синтаксиса и документации

### 1. Базовая сверка с эталоном
- [x] Сравнить текущие директории `supra/move_workspace/lottery_core`, `lottery_support`, `lottery_rewards` с эталонными файлами в `supra/move_workspace/lottery_backup`.
- [x] Проанализировать синтаксис эталонных файлов, чтобы заранее поймать несовместимые конструкции.
- [x] Зафиксировать различия в структурах, модулях, комментариях и сигнатурах функций.

### 2. Коррекция Move‑контрактов
- [x] Удалить запрещённые модификаторы (`public` у структур), восстановить корректные трейты `has` и порядок объявлений.
- [x] Проверить сигнатуры функций: убрать лишние `&mut`, вернуть правильный порядок аргументов и возвращаемых типов.
- [x] Проследить, чтобы не осталось не-ASCII символов и недопустимого синтаксиса (`let mut` и т. п.).

### 3. Проверка дополнительных пакетов
- [x] Повторить коррекции для `lottery_support` и `lottery_rewards`, учтя межмодульные зависимости и названия capability.
- [x] Обновить тестовые утилиты и хелперы, чтобы они совпадали с эталонной версией.

### 4. Обновление документации
- [x] Перепроверить `docs/testnet_runbook.md`, `SupraLottery/README.md` и сопутствующие инструкции.
- [x] Обновить описания capability, процедур деплоя и сборки, добавить примечания о требованиях Move-компилятора.
- [x] Синхронизировать примеры команд с актуальными путями и пакетами.
